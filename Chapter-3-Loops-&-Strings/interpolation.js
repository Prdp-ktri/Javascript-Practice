let obj = {
  name: "Pradeep Khatri",
  age: 28,
};
console.log(obj.name + " is " + obj.age + "years old.");
console.log(`${obj.name} is ${obj.age} years old.`);

let str = `Hum aapka \t jeevan`;
console.log(str);
